1. Number of CPUs
2. Target CPU utilization (rane[0, 1])
3. Wait time (W)
4. Compute time (C)

Formula to determine the optimal size of the thread pool

**************************************************************************
Number of threads = Numbers of CPUs * Target CPU utilization * ( 1 + W/C)
**************************************************************************
